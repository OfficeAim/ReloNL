# Dependencies
node_modules/
.npm

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Environment secrets — NEVER commit these
.env
.env.local
.env.*.local
supabase/.env
*.pem
*.key

# Supabase local dev (keep functions and schema, ignore local state)
supabase/.branches
supabase/.temp

# Zip artifacts (kept locally, not in repo)
*.zip

# Logs
*.log
npm-debug.log*
